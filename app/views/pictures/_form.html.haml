= form_for @picture do |f|
  - if @picture.errors.any?
    #error_explanation
      %h2= "#{pluralize(@picture.errors.count, "error")} prohibited this picture from being saved:"
      %ul
        - @picture.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :pic
    = f.file_field :pic
    
  .actions
    = f.submit 'Save'


/ = form_for @lang do |f|
/   - if @lang.errors.any?
/     #error_explanation
/       %h2= "#{pluralize(@lang.errors.count, "error")} prohibited this picture_lang from being saved:"
/       %ul
/         - @lang.errors.full_messages.each do |msg|
/           %li= msg

/   .field
/     = f.label :title
/     = f.text_field :title

/   .actions
/     = f.submit 'Save'