%p#notice= notice

%p
  %b Title:
  = @binary.name
%p
  - @binary_langs.each do |pl|
    %p
      %b the title:
      = pl.title
      %br
      %b the translation:
      = pl.lang
- if user_signed_in?
  %p
    %b Notes:
    %br
    = @binary.notes

= link_to 'Edit', edit_binary_path(@binary)
\|
= link_to 'Back', binaries_path
