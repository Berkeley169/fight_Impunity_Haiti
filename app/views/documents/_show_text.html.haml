- @document.text_langs.each do |dl|
  %p
    = dl.plain_text.html_safe
    / need to show iff text file is not missing
    %b File:
    = image_tag dl.txt.url
    %br

%p
  - @document.subtype_fields.keys.each do |f|
    %p
      %b= "#{f.to_s.titleize}"
      = @document.subtype_fields[f]

