.content
  %h1=@tag.send(locale_to_word)
  .tag_sidebar
    = render :partial => 'tag_button', :collection => @tags, :as => :t
  = render :partial => 'document_quickview', :collection => @documents, :as => :document

%br